# Master Roadmap

The standing plan for the Arabic-learning platform. Every milestone scope doc cites this for "where does this fit and what may it not touch." Design detail lives in [CURRICULUM_ARCHITECTURE.md](CURRICULUM_ARCHITECTURE.md); milestone workflow lives in the Master Standards (`feedback_master_standards` in project memory).

## Philosophy

1. **Build the educational engine first. Populate it second. Personalise it third. Polish the product last.**
2. **Local-first.** Everything works single-device with no backend until there is per-user state genuinely worth syncing (M22).
3. **Quality over quantity.** No AI-generated mountain of questionable Arabic. Every Arabic string is checked for spelling, ḥarakāt, grammar, transliteration, meaning, natural usage, and level-appropriateness.
4. **Education before monetisation.** Design the curriculum on its merits; decide what is free vs. premium only once the product is genuinely valuable.
5. **Honesty about limits.** The system never fabricates a score for something it cannot measure (free writing, speaking fluency). It says "self-assessed" or "insufficient evidence" instead.

## Position

`recover/m21-full-history` branch, base `main`. **M6–M19 + the .5 batch (M14.1 / M14.5 / M15.5 / M19.5 / M20.5 / M21.5) + M20 (complete) + M20.9 merged and live.** M21 batches 1–9 merged: A2 grammar, numbers 11–99, telling time, prices, dates, root & pattern, narrative/everyday-exchanges vocabulary, reading short paragraphs, and shopping/directions/everyday-exchanges dialogues — **all six A2 units (`a2-u1`–`a2-u6`) are now fully complete, closing A2 entirely.** This satisfies [reference/iraab-audit.md](reference/iraab-audit.md) §9 Implementation Gate prerequisite 1 ("A2 not closed"). **M21.6 (the parse/label exercise-type engine — the same bounded milestone the held `m27.0_parse-engine_scope.md`/`m27_iraab_programme_scope.md` docs independently re-drafted before this history was recovered) is merged and live**, satisfying prerequisite 3 ahead of schedule. B1 grammar (prerequisite 2) now covers **derived verb forms II–X (eight of ten, `b1-u1`, one lesson per form), subject- and object-relative clauses (singular + plural), all ten sisters of إنّ وأخواتها, joining them into paragraphs, and the plural possessive suffixes — `b1-u2` now complete short of dual relative pronouns and the feminine-plural/dual possessive suffixes (deliberately deferred, like Form IX). **The first B2 unit is open**: `b2-u1` (`gr:case-system`, batch 20) actively names رَفْع/نَصْب/جَرّ on regular nouns, reusing `b1-u7`'s own seven role-tagged sentences with `case` now added — the `label` engine's role+case tier, dormant since M21.6, finally has real content, and grammatical role recognition (`b1-u7`, M21.7 — the label engine's first real content, satisfying the audit's §6 "Recommended First iʿrāb Entry Point")** — see the M21 row's history note for the recovery this state reflects. Backend is also **locked in: Supabase** (M21.9) — M22 (student accounts) and M23 (subscriptions) build on the schema/RLS/Stripe-via-Edge-Functions groundwork laid there.

Phases 1–2 (M13–M15) build the frame. Phase 3 (M16) fills it. Phases 4–6 (M17–M19) make learning findable, provable, and durable. Phase 7 (M20–M21) produces content. Phase 8 (M22–M23) adds accounts and monetisation. Then the UI/UX pass, the AI tutor layer, and the long tail of content.

---

## The milestones

| M | Name | One line | Proven done when | Depends on |
|---|---|---|---|---|
| **M13** | Native-audio prep | Manifest + resolver + TTS fallback, inert behind a flag. | ✅ merged | — |
| **M14** | Learning-object core | One `lexemes` table + typed `LETTERS`/`MARKS`/`SYLLABLE_OBJECTS`/`GRAMMAR_POINTS`/`TEXTS` in `content/*.json`, compiled to the app. Behaviour-preserving. | ✅ merged | — |
| **M14.1** | UI baseline (mobile-first, RTL, accessible) | Audit-and-harden pass on the *existing* UI — WCAG 2.5.5 touch targets, measured contrast, explicit `dir` alongside `lang`, focus-visible states, a documented breakpoint set — before M15/M16 build more UI on top of it. Not a redesign; M24 owns that. | ✅ merged (PR #14) — see [m14.1_ui_baseline_scope.md](m14.1_ui_baseline_scope.md), [UI_BASELINE.md](UI_BASELINE.md), [content/m14.1-a11y-review.md](content/m14.1-a11y-review.md) — `tools/a11y-audit.js` clean (0 touch-target, 0 contrast findings) across 2 themes × 6 breakpoints; `tools/qa-harness.js` 31/31 | M14 |
| **M14.5** | Content validation framework | `tools/build-content.js --lint` (structural: dangling ids, duplicates, ḥarakāt coverage) + `tools/qa-harness.js` (Playwright regression battery), formalizing the manual QA process used for every milestone so far. First npm devDependency. | ✅ merged (PR #14) — see [m14.5_content_validation_scope.md](m14.5_content_validation_scope.md) — 30/31 checks pass (the 1 failure was a genuine pre-existing gap, closed by M14.1) | M14 |
| **M15** | Skills · levels · learning graph | 8 strands, 7 levels (PRE_A1→C2), **all 56 Arabic-specific can-do descriptors** (each cited to an external framework), `prereqs` edges among M14 objects, honest `deriveLevel(skill)`. Progress view gains a per-strand panel; no other UI change. | ✅ merged (PR #10) — 234 objects tagged, 62 acyclic level-monotonic edges, `deriveLevel` returns level + confidence or "insufficient evidence", "Your skills" panel live. | M14 |
| **M15.5** | Audio / pronunciation foundation | Recorded-vs-synthesized honesty indicator on every `buildAudioControl()`; A/B "compare with model" toggle in the recorder. M13's manifest/resolver already covered all content (not a bounded tranche) — turned out to need no extension. No scoring — stays inside the existing deferral. | ✅ merged (PR #14) — see [m15.5_audio_pronunciation_foundation_scope.md](m15.5_audio_pronunciation_foundation_scope.md), [content/m15.5-audio-coverage-review.md](content/m15.5-audio-coverage-review.md) — `tools/qa-harness.js` 31/31, `tools/a11y-audit.js` clean | M13, M15 |
| **M16** | Curriculum spine | The real `curriculum` tree (levels → units → lessons) wired to objects and levels; Learn view "start where you fit"; lessons declare `objectives`; the 7-stage lesson generator. The full A2–C2 content *map* placed by level. | ✅ merged (PR #12) — `content/curriculum.json` (37 units / 18 lesson nodes), `generateLessonSteps` v1 + 2 generated A1 lessons, `deriveLevel` introduced-coverage bridge (capped A1), Learn view rebuilt. | M15 |
| **M17** | Placement / diagnostic | Per-strand adaptive-lite diagnostics for the reliably-measurable strands, self-report for speaking/writing, honest confidence, learner override. **Rides on the existing MCQ / listen-identify primitives** — does not build a new item engine. | ✅ merged (PR #13) — `buildPlacementItems` generates items from objects, adaptive walk with per-strand ceiling ("A2 or above"), `#view-placement`, `deriveLevel` placement tier, 3 entry points. | M15, M16 |
| **M18** | Assessment framework | `exerciseTypes` registry (parallel to `stepRenderers`); migrate MCQ in; add `match` / `order` / `cloze` / `build-word` / `build-sentence` / `dictation` / `transform`. Each item references learning objects; results feed the review store. | ✅ merged (PR #15) — registry + `choice`/`match`/`cloze`/`build`/`order`, all generated from objects; `InteractionEvent` → `progress.interactionLog` (ring buffer, cap 400); generator emits varied items; `build` gives `writing` its first machine-checkable signal. `dictation`/`transform`/`multi-choice`/`short-write` deferred. | M15 |
| **M19** | Review / retention | `InteractionEvent` + per-object `ReviewState` (backfilled from existing quizzes); an SM-2-lite scheduler as a pluggable module; a mistake log; review queues. The crude `mastered` toggle becomes `learned → practised → weak → forgotten → reviewed → retained`. | ✅ merged (PR #16 + fix #17) — `scheduler.next()` frozen/pure/replaceable; `ReviewState` derived + cached via `reviewCursor`, seeded from `mastered`; six-state lifecycle; four queues; `#view-review` runner + Practice "Time to review" card; `deriveLevel`'s graded tier → `retainedSet()` so every strand can reach a level from graded practice. | M18 |
| **M19.5** | Local learner state (IndexedDB) | An unbounded, queryable `interactionEvents` companion store in IndexedDB alongside (not replacing) M19's capped 400-event `interactionLog` — `progress` stays synchronous/localStorage-backed. Scope narrowed once grounded in the real merged M19, which already solved the unbounded-blob problem pragmatically; see the scope doc §1. `safeIdb*` wrappers, versioned backfill, `getFullInteractionHistory()` for M21.5. | ✅ merged (PR #14) — see [m19.5_local_learner_state_scope.md](m19.5_local_learner_state_scope.md), [content/m19.5-local-learner-state-review.md](content/m19.5-local-learner-state-review.md) — `tools/qa-harness.js` 31/31, `tools/a11y-audit.js` clean | M19 |
| **M20** | Content pipeline + complete A1 | `tools/build-content.js` grows a schema + **linguistic linter** (ḥarakāt coverage, transliteration↔Arabic consistency, register, level fit); migrate all existing content into it; then author a **complete, heavily-QA'd A1 across all 8 strands** as the reference level. Internal gate: pipeline proven → then author A1. | ✅ Phase A (PR #18). ✅ B1 grammar (#21). ✅ B2 core vocab (#22). ✅ B3 rest of A1 vocab (#23) — 94 lexemes closing most of `wordlists/a1.json`: school, work, places, shopping, travel, directions, body/health, weather, question words, conjunctions, subject pronouns. ✅ B3 residual cleanup — 7 more lexemes (young/old-person/old-thing, park, work/job, above/on, to-do/to-make) closing the gaps a plain English-gloss diff against the wordlist missed the first time; wired into the existing generated lessons, no new lesson nodes. **`wordlists/a1.json` checklist now fully closed — 317 lexemes.** ✅ B4 (A1 texts, #25) — 20 fully-vowelled A1 texts (16 graded sentences + 4 mini-dialogues), built only from the merged A1 vocab + grammar, `source:"m20"`; 3 data-authored lessons on `a1-u1` / `a1-u4`. Verbless throughout (A1 has no verb-sentence grammar — narrative reading is post-A2). ✅ B5 (A1 writing, #27) — 3 data lessons (`a1-writing-join-spell`, `a1-writing-dictation`, `a1-writing-sentences`) on `a1-u1` / `a1-u3`: positional-form joining + word spelling + dictation (`build`/grapheme, with a 4-line additive `item.audio` hook) + subject-predicate sentence building (`build`/word on the batch-4 texts). Compiler gains `fromObjectives` on `exercise` steps. `"writing"` added to 23 exercise-target objects so `deriveLevel("writing")` can finally leave "insufficient evidence" → **`A1 · provisional`**. 0 new Arabic. ✅ B6 (wire `a1-u5`) — one new grammar point, `gr:counted-noun-intro` (1-2 use the noun's own singular/dual form; 3-10 take a plural noun; the reversed-agreement rule is deferred, honestly, to `a2-u4` which already owns it), 3 example sentences reusing existing vocabulary, one lesson (`a1-numbers`) on the already-existing A0 numbers 0-10. `a1-u5` flips `planned` → `available`. Zero new lexemes. **M20 Phase B — and M20 itself — complete.** | M15–M19 |
| **M20.5** | Arabic language tools | `normalizeArabicInput()` (alef/tāʾ marbūṭa normalization, for M18's future free-text grading); curated `content/roots.json` for confusable-root distractors; `deriveReducedForm`/`deriveUnvowelledForm` wired into M14.5's linter as a drift check on `texts.json`. Explicitly not a general morphological analyzer or dictionary spellchecker. | ✅ merged (PR #14) — see [m20.5_arabic_language_tools_scope.md](m20.5_arabic_language_tools_scope.md), [content/m20.5-language-tools-review.md](content/m20.5-language-tools-review.md) — 11 verified root clusters (24 lexemes); `build-content.js --lint`'s texts.json drift check is clean; `tools/qa-harness.js` 31/31, `tools/a11y-audit.js` clean. Built against the current lexeme set directly rather than waiting on M20's "complete A1" — its real code dependencies (`content/lexemes.json`, `content/texts.json`, M14.5's `--lint`) already existed | M20 |
| **M20.9** | Alphabet articulation diagrams | Wires the already-reviewed articulation illustration set (all 28 letters, real anatomical photo + marked constriction points, phonetics audited) into the real Alphabet detail view: a collapsed-by-default "🗣️ How it's made" section per letter (`ARTICULATION` lookup, hand-authored directly in `index.html` per the scope doc's own reasoning — not round-tripped through `content/letters.json`), plus a "Where each sound is made" overview map in the Alphabet tab with 28 real jump-links. `buildAudioControl()`/`playArabicAudio()` untouched. The overview's per-letter jump-links live as real HTML buttons below the diagram, not text crammed inside the SVG — a dense diagram can't give 28 individual glyphs genuine 44×44 touch targets without either overlapping neighbors or ballooning the whole map. | 🧪 implemented, pending review — see [m20.9_alphabet_articulation_diagrams_scope.md](m20.9_alphabet_articulation_diagrams_scope.md) — `tools/qa-harness.js` 57/57, `tools/a11y-audit.js` clean (touch targets + contrast), `npm run content:check` unchanged (441 objects, confirms `content/letters.json` untouched) | M20 |
| **M21** | Content scale-out | A2 → B1 → B2 → C1 → C2, level by level, against a stable engine. The advanced/literary end (الأدب، الشِّعر، البلاغة، النقد، الفكر، الصحافة، اللغة الأكاديمية) is the **top of this ladder**, with specialised sub-tracks where the pedagogy differs from language acquisition. **The beginning of a continuous content operation, not a task with an end.** Each level coherent and QA'd before the next starts. | ✅ Batch 1 (A2 grammar + `a2.json` wordlist, PR #30) — `content-lint.js`'s level-fit and ḥarakāt-coverage rules extended to A2 (proven with 2 new fixtures); `wordlists/a2.json` authored (42-entry checklist across numbers-extended/narrative/everyday-exchanges, for later vocab batches — zero new lexemes needed by this batch itself, since all 29 verb lexemes already cite the past-tense "he did X" dictionary form). Three new grammar points — `gr:verbal-sentence` (past-tense Form I across the full person paradigm + VSO word order), `gr:kana` (كَانَ moves a nominal sentence into the past; irregular hollow-root conjugation), `gr:idafa` (the genitive construct) — 9 new example texts from existing vocabulary only. `gr:present-tense`'s examples additively extended from 3 persons to all 7 (أنا/أنتَ/أنتِ/نحن added). Two new lessons (`a2-verbal-sentence`, `a2-idafa`). `a2-u1` and `a2-u2` flip `planned` → `available` — **A2's first two available units.** ✅ Batch 2 (Numbers 11–99, PR #32) — reframed from the milestone doc's generic "A2 core vocabulary" into completing `a2-u4`'s cardinal-numbers half concretely, since (checked, not assumed) `a2-u4`/`a2-u5`/`a2-u6` had no lesson nodes yet to wire loose vocabulary into, unlike A1's vocab batches which slotted into M16's pre-existing generated lessons. 18 new lexemes (eleven through ninety, hundred). One new grammar point, `gr:numbers-11-99` — pays off `gr:counted-noun-intro`'s own explicit deferral of the reversed-agreement rule to this unit: 11/12 as irregular compounds, 13–19 extending the known reversed-agreement rule with a normally-agreeing "-teen" marker, 20–90 fully gender-invariant, 21–99 compounding, and مِائَة (100) governing a genitive noun (a third pattern again) — 7 example texts (more than the usual 3, given the density of distinct sub-rules). One new lesson (`a2-numbers-11-99`). `a2-u4` flips `planned` → `available`, blurb narrowed to match only what shipped — telling time, dates, and prices (needing ordinal numbers, not yet checklisted) explicitly deferred to a small follow-up. ✅ Batch 3 (Telling time, PR #33) — a2-u4's second lesson, delivering the follow-up batch 2 deferred. Checked the real lexicon first: `سَاعَة` already carried a note flagging its "clock" reuse, `دَقِيقَة`/"What time is it?" already existed, half/quarter were still sitting unauthored exactly where batch 2 left them. 15 new lexemes — `وَاحِدَة` (one o'clock's cardinal exception), ordinals second through twelfth (feminine only, the masculine form flagged out of scope), a new particle إِلَّا ("except/minus"), half/quarter. One new grammar point, `gr:telling-time`: a definite noun + definite ordinal functions as a complete phrase (السَّاعَةُ الثَّانِيَةُ), one o'clock's cardinal exception, the ordinal teen-compounds staying indeclinable exactly like the cardinal teens, وَ for "past" and إِلَّا for "to" — 6 example texts. One new lesson, `a2-telling-time`, `a2-u4`'s second lesson (unit stays `available`, blurb narrows further). Also fixed a real `content-lint.js` gap found while authoring: the ḥarakāt check's excused-ل logic only recognized word-initial الـ, not الـ with a single attachable prefix glued on (وَالنِّصْفُ) — generalized and proven with 2 new fixtures. ✅ Batch 4 (Prices, PR #34/#35) — `a2-u4`'s third lesson. 4 currency nouns (`دُولَار`, `رِيَال`, `دِينَار`, `جُنَيْه`) — everything else (`سِعْر`, `بِكَمْ؟`, `بِكَمْ هَذَا؟`) already existed at A1. **No new grammar point** — prices are just [number] + [currency noun] applying the two counting rules already taught (3–10 plural genitive, 11–99 singular accusative, مِائَة's genitive iḍāfa), the first batch this session that's pure vocabulary application. 3 example texts, one per case pattern. One new lesson (`a2-prices`). Scoped alongside batch 5 (dates) in one doc but shipped as its own smaller PR first, per the approved split. ✅ Batch 5 (Dates, PR #35) — `a2-u4`'s fourth and final lesson; the unit's original blurb ("11–99, gender polarity, dates, prices, and the clock") is now delivered in full, the second A2 unit (after `a2-u1`/`a2-u2`) to close out entirely. 24 new lexemes: 10 masculine ordinals 1st–10th (reusing the same roots batch 3's feminine ordinals already taught — `ثَانِي`, `ثَالِث`, `رَابِع`…, since `يَوْم` is masculine unlike `السَّاعَة`), 12 month names (Western-transliterated set, flagged as a regional choice), `التَّارِيخ` and its question form. One new grammar point, `gr:dates`: [definite ordinal] + `مِنْ` + [month] as a complete phrase, same elliptical pattern as `gr:telling-time`; `أَوَّل` (1st) flagged as its own irregular word, `ثَانِي` (2nd) as a defective noun (`ثَانٍ`/`الثَّانِي` alternation) — 4 example texts including one full sentence with a linking `هُوَ`. One new lesson (`a2-dates`). Ordinals deliberately capped at 1st–10th (11th–31st reuse the same compounding pattern already taught, just masculine — authoring all 31 would be padding) and full calendar years deferred (needs "thousand", not yet in the lexicon). ✅ Batch 6 (Root & Pattern, PR #36) — `a2-u3`'s one and only lesson, the third A2 unit to close out completely. **Zero new lexemes**: wires all 24 lexeme references across the 11 already-verified root clusters in `content/roots.json` (built in M20.5 for quiz distractors, never loaded into the compiled app until now) into a real lesson for the first time. One new grammar point, `gr:root-pattern` — the root-and-pattern derivational system (a root's three consonants carry a core meaning; a pattern poured into it adds a grammatical role) — 4 example texts, each pairing two cluster-mates in a sentence built only from already-taught vocabulary and grammar: كِتَاب/مَكْتَبَة (ك-ت-ب), طَعَام/مَطْعَم (ط-ع-م), جَدّ/جَدِيد (ج-د-د — a real, cited root family despite looking unrelated in translation), and الْخَمِيس/خَامِس (خ-م-س — tying together three separate batches' vocabulary: numbers, weekdays, and batch 5's masculine ordinals). `a2-u3` flips `planned` → `available`; its blurb corrected to name the words the lesson actually teaches (كِتَاب, كَتَبَ, مَكْتَبَة) rather than the different placeholder examples (كَاتِب, مَكْتَب) written speculatively back at M16, before any A2 content existed. ✅ Batch 7 (Narrative & everyday-exchanges vocabulary, PR #37) — a pure vocabulary batch, no lesson, no new grammar. Closes the last two unclosed A2 checklist topics in `content/wordlists/a2.json`: `narrative` (10 entries) and `everyday-exchanges` (12 remaining after batch 4's 4 currencies). Checked the lexicon first: "I would like..." and "there is/are" were already covered by existing lexemes (`lex:ver-16` أَرَادَ's documented present tense, `lex:pla-17` هُنَاكَ's existing note) — zero new lexemes needed for either. 21 new lexemes across particles (ثُمَّ, أَثْنَاءَ, بَيْنَمَا, and كُلّ — the last genuinely dual-behaviour: "every X" before an indefinite noun, "all of X" before a definite one, either way an iḍāfa first term), fixed expressions (أَيْضًا, أَخِيرًا, فَجْأَةً, أَوَّلًا reusing batch 5's أَوَّل, ذَاتَ يَوْمٍ, طَبْعًا, لَا بَأْسَ reusing `gr:negation-a1`'s already-stated rule, هَلْ عِنْدَكَ...؟ reusing `lex:ver-17`'s عِنْدَ paradigm, مُمْكِن...؟ left deliberately unanalyzed since the subjunctive it could invoke is a B1 topic, and هَذَا كُلُّ شَيْءٍ), 4 shopping nouns (نَقْد, بِطَاقَة اِئْتِمَان, مَقَاس, خَصْم), one Form-II verb cited as plain vocabulary (جَرَّبَ — its derivational pattern stays B1's job), and one school-topic noun (قِصَّة). One supporting lexeme beyond the literal checklist, شَيْء ("thing", needed to build the "that's all" phrase), retroactively added to `wordlists/a2.json` to close its level-fit warning, the same established pattern as every prior batch's checklist gaps. Also recorded one `content/_lint-allow.json` exception (`lex:prt-26`, ثُمَّ): the "length" heuristic only counts base Arabic letters and misses that a shadda-doubled consonant plus a 2-character transliteration digraph (ث→th) legitimately inflate a short word's translit length — a documented, reviewed false positive, not a content error. `a2-u5`/`a2-u6` stay `planned`; this batch only stocks their future vocabulary. ✅ Batch 8 (Reading Short Paragraphs, PR #38) — `a2-u5`'s one lesson, the fourth A2 unit to close out completely. The first genuinely connected, multi-sentence text in the project — every one of the prior 91 texts was a standalone sentence. One new `textType`, `"paragraph"`, with its own `sentences` array (`{ar, translit, en}` per sentence), mirroring how `"dialogue"` already got its own `turns` array rather than overloading the per-word `words` field at the wrong granularity — confirmed by grepping every tool file that `textType` isn't validated or branched on anywhere except one harmless fallback in `build-audio-manifest.js` (any non-`"dialogue"` value already falls through to the "sentence" audio bucket), so this needed no engine change. Two new texts, both built from vocabulary and grammar already taught (zero new lexemes, zero new grammar): `txt:read-para-trip` (a student buys a ticket, travels by train, reads during the trip, and returns home — using أَوَّلًا، ثُمَّ، أَثْنَاءَ، أَخِيرًا) and `txt:read-para-garden` (one day the weather was beautiful; while a man was in the garden he suddenly saw a plane — using ذَاتَ يَوْمٍ، بَيْنَمَا، فَجْأَةً، طَبْعًا), together putting 8 of batch 7's 21 narrative-connector words to their first real use. An early draft's use of the untaught circumstantial accusative (ḥāl) was caught and dropped before implementation, replaced with a second `كَانَ` + accusative-predicate sentence reusing an already-taught pattern instead. One new lesson (`a2-reading-paragraphs`) with three comprehension-style `practice-choice` questions (main idea, detail-sequencing, cross-paragraph) rather than grammar-recognition — the first time this project's practice questions have tested reading comprehension instead of a grammar point, matching the unit's own "separating main idea from detail" blurb. `tools/qa-harness.js`'s "catalog lesson walks to completion" check exercised the new `sentences`-array rendering path for the first time, cleanly. ✅ Batch 9 (Shopping & Directions, `a2-u6`) — the sixth and final A2 unit, closing A2 entirely. Scoped in [m21b9_a2_everyday_exchanges_scope.md](m21b9_a2_everyday_exchanges_scope.md), driven by [reference/iraab-audit.md](reference/iraab-audit.md) §9's Implementation Gate (prerequisite 1 for the iʿrāb programme). Checked the lexicon first: all 13 of batch 7's idle everyday-exchanges lexemes, the currencies from `a2-prices`, and the directions set (`يَمِين`/`يَسَار`/`إِلَى الْأَمَام`/`هُنَا`/`هُنَاكَ`/`قَرِيب`/`بَعِيد`/`شَارِع`, all already sitting unused at A0/A1) were already fully stocked — **zero new lexemes**, same discipline as batches 6 and 8. The one real gap — no imperative mood taught anywhere, needed for authentic "turn left"-style commands — was resolved per the scope doc's §3 decision (b): stay inside existing grammar and phrase directions as location statements (`gr:nominal-sentence` + `gr:core-prepositions`, "الْبَنْكُ قَرِيبٌ، عَلَى الْيَسَارِ" = "the bank is near, on the left") rather than add a new grammar point — **zero new grammar**, deferring the imperative to wherever the roadmap needs it next. Two new dialogue texts (`txt:read-dlg-shopping`, `txt:read-dlg-directions`, reusing the existing `dialogue`/`turns` shape from M20 — no engine change): a shopping exchange (price, trying something on, a discount, cash vs. credit card, "that's all") and a directions exchange (asking after two places, one near, one far, reusing batch 5's masculine ordinal `الثَّانِي` for "the second street"). One new lesson (`a2-everyday-exchanges`), listening/speaking-flavored per the unit's own skill tags — dialogue playback plus four `practice-choice` comprehension questions, matching batch 8's comprehension framing over grammar-recognition. `a2-u6` flips `planned` → `available`, blurb's "In development" clause dropped. `npm run qa` 66/66, `node tools/a11y-audit.js` clean. ⚠️ **History note (2026-09-11 – 2026-09-14):** `main` was briefly force-pushed back to its 2026-09-08 08:48 state by a local "restore project on ubuntu" push, discarding PRs #39–#48 (M21.6's engine, batches 11–13, an A2 writing batch). Nothing was lost permanently — the work survived on its GitHub branches — but local sessions re-derived a slower, partial version of the same ground (`m21b10`/`m21b11` as Form II alone / Form III alone) before the loss was noticed and the full history recovered on branch `recover/m21-full-history`. The batch list below reflects the **recovered, final state**, not the intermediate redo. ✅ **M21.6 — parse/label exercise-type engine** (recovered; see the "After M23" table below, moved up from the old M27.0 slot it was drafted under before this history was recovered) — `exerciseTypes.label`, dormant until real content exists. ✅ Batch 10 (A2 writing: sentences + dictation) — `a2-writing-sentences` (`a2-u1`) and `a2-writing-dictation` (`a2-u4`), reusing existing grammar-point texts and A2 vocabulary; no new Arabic. ✅ Batches 11–12, recovered + restructured (Derived Verb Forms II–X, `b1-u1`) — see [m21b11-12_b1_derived_verb_forms_recovery_scope.md](m21b11-12_b1_derived_verb_forms_recovery_scope.md) for the full account. The originally-merged version (PRs #43–#46) taught five forms in one lesson and three in a second; recovering it, that pacing was judged a real regression against this project's own one-form-per-lesson discipline and against how reference grammars sequence derived forms, so delivery was **re-split into eight lessons, one per form** (`b1-derived-forms-ii` … `-viii`, `-x`), each still built from the recovered content: `gr:derived-verb-forms` extended to cover all eight forms' shape + meaning tendency, 8 example texts, and 3 new B1 lexemes (`اِنْفَتَحَ`/`اِنْغَلَقَ`, Form VII minimal pair with already-known `فَتَحَ`/`أَغْلَقَ`; `تَكَاتَبَ`, Form VI) — seven of the eight forms taught reuse vocabulary already in the lexicon (`جَرَّبَ`, `سَافَرَ`, `أَرَادَ`, `أَغْلَقَ`, `تَكَلَّمَ`, `اِشْتَرَى`, `اِسْتَيْقَظَ`). `b1-u1` blurb names all eight forms; Form IX (colours/physical defects) stays deferred past B1. ✅ Batch 13 (Relative Clauses, `b1-u2`) — recovered unchanged: `الَّذِي`/`الَّتِي`, subject-relative only, four worked sentences (definite + indefinite antecedents), `b1-u2` flips `planned` → `available`. `npm run content:check` clean at 591 objects (404 lexemes, 20 grammar points, 106 texts, 37 units, 63 lessons); `npm run qa` / `node tools/a11y-audit.js` — see the recovery branch. ✅ **M21.7 (Grammatical Role Recognition, `b1-u7`)** — the label engine's first real content, six weeks after M21.6 shipped it dormant. Scoped in [m21.7_b1_role_recognition_scope.md](m21.7_b1_role_recognition_scope.md). **0 new lexemes, 0 new texts, 0 new grammar points**: seven existing `content/texts.json` entries (already-taught A1/A2 examples for `gr:nominal-sentence`/`gr:verbal-sentence`/`gr:idafa`) gain an additive `parse` array naming the five roles those grammar points' own rule text already uses — مبتدأ، خبر، فاعل، مفعول به، مضاف إليه. New unit `b1-u7` ("Grammatical Roles & Iʿrāb Readiness") and one lesson, `b1-role-recognition`, hand-authored directly against `exerciseTypes.label`'s literal schema (`sentenceHtml`/`options`/`answer` — `tools/build-content.js`'s `fromObjectives` expansion only covers `kind:"build"`, confirmed before authoring rather than extending it speculatively): five `label` steps, one per role, then two `practice-choice` questions on the two iḍāfa sentences that each carry two independently-labelled roles. `b2-u1`'s `prereqs` gains `b1-u7` alongside `b1-u2` — full case-marking parsing depends on role-recognition being solid first. **Zero `index.html`/`tools/*.js` engine changes.** One authoring correction made while implementing: the lesson title and two explanation strings, as originally scoped, packed enough Arabic to cross `qa-harness.js`'s 50%-Arabic "dedicated content" threshold in a plain-English container without `lang`/`dir` — caught by the lang/dir coverage check, fixed by citing fewer terms / adding more English framing, no schema or content-substance change. `npm run content:check` clean at 591 objects (38 units, 64 lessons); `npm run qa` 79/79; `node tools/a11y-audit.js` clean. ✅ **Batch 14 (`إنّ وأخواتها`, first look, `b1-u2`)** — the second `b1-u2` lesson, named as batch 13's own follow-up. Scoped in [m21b14_b1_inna_wa_akhawatuha_scope.md](m21b14_b1_inna_wa_akhawatuha_scope.md). Covers three of the ten sisters of إنّ: **إِنَّ** (new, emphatic opener), **أَنَّ** (new, subordinates a nominal clause as "that" — explicitly distinguished from the unrelated subjunctive أَنْ), and **لِأَنَّ** ("because," already in the lexicon since A1 as `lex:prt-17` but never formally connected to the family — annotated with a `notes` field, not replaced, the same "found it already there" pattern batches 11/12 used for hidden derived verb forms). One new grammar point, `gr:inna-sisters`, framed explicitly as `gr:kana`'s case-shift mirror image (كَانَ pushes the *predicate* to accusative; إنّ's family pushes the *subject* instead, leaving the predicate nominative) — names but defers the four remaining sisters (كَأَنَّ, لٰكِنَّ, لَعَلَّ, لَيْتَ) and attached-pronoun forms. 3 new example texts, all built from already-taught vocabulary (weather/beautiful from batch 8's paragraph reading, سَمِعَ, happy/warm). One new lesson, `b1-inna-sisters`, `b1-u2`'s second. `b1-u2` blurb narrows to name what's shipped across both its lessons. `npm run content:check` clean at 597 objects (406 lexemes, 21 grammar points, 109 texts, 38 units, 65 lessons); `npm run qa` 80/80; `node tools/a11y-audit.js` clean. ✅ **Batch 15 (`إنّ وأخواتها`, completing the set, `b1-u2`)** — scoped in [m21b15_b1_inna_sisters_complete_scope.md](m21b15_b1_inna_sisters_complete_scope.md), named as batch 14's own follow-up. The remaining four of the ten sisters: **كَأَنَّ** ("as if," comparison), **لٰكِنَّ** (the *heavy* case-marking "but" — a genuine minimal pair against the already-known *light* لَكِنْ, which never shifts case), **لَعَلَّ** ("perhaps," hope/expectation), **لَيْتَ** ("would that / I wish," an irrealis wish for something unlikely, explicitly contrasted with لَعَلَّ's more realistic hope). `gr:inna-sisters` grown additively (same `gr:` id, `rule` extended, `examples` grown 3→7, two new `commonErrors` entries) rather than a second grammar point — the same discipline `gr:derived-verb-forms` used across `b1-u1`. 4 new example texts continuing batch 14's weather theme, reusing only already-taught vocabulary beyond the four particles themselves. One new lesson, `b1-inna-sisters-2`, `b1-u2`'s third — bundles all four sisters in one lesson (matching batch 14's own shape, not `b1-u1`'s one-form-per-lesson split), since unlike the derived verb forms these four share one already-taught rule rather than each introducing a new morphological pattern. `b1-u2` blurb states all ten sisters of إنّ are now named (seven with a full worked example); attached-pronoun forms and object-relative clauses stay open. `npm run content:check` clean at 605 objects (410 lexemes, 21 grammar points, 113 texts, 38 units, 66 lessons); `npm run qa` 81/81; `node tools/a11y-audit.js` clean. ✅ **Batch 16 (`إنّ وأخواتها`, attached-pronoun forms, `b1-u2`)** — scoped in [m21b16_b1_inna_attached_pronouns_scope.md](m21b16_b1_inna_attached_pronouns_scope.md). Every sister of إنّ can take an attached pronoun as its subject instead of a following noun — not a new suffix set, the exact same possessive suffixes `gr:attached-possessive` already taught (ـهُ, ـهَا, ـكَ, ـكِ), now attaching to a particle instead of a noun. **0 new lexemes** — this is a mechanic, not vocabulary. `gr:inna-sisters` grown again (same `gr:` id, one new `rule` paragraph cross-referencing `gr:attached-possessive` directly rather than re-teaching the suffixes, `examples` grown 7→10, one new `commonErrors` entry, `prereqs` gains `gr:attached-possessive`); the 1st-person irregularity (إِنَّ + ي → إِنِّي, not إِنَّنِي) is flagged, not taught, the same "cited, not explained" treatment `أَرَادَ`'s hollow root got. 3 new example texts showing the mechanic on three different sisters (إِنَّ, أَنَّ, لِأَنَّ) so it reads as one rule, not three. One new lesson, `b1-inna-attached-pronouns`, `b1-u2`'s fourth. `b1-u2` blurb states إنّ's family (noun and pronoun subject both) fully covered; only object-relative clauses and paragraph-joining remain open. `npm run content:check` clean at 608 objects (410 lexemes, 21 grammar points, 116 texts, 38 units, 67 lessons); `npm run qa` 82/82; `node tools/a11y-audit.js` clean. ✅ **Batch 17 (relative clauses, completing the set, `b1-u2`)** — scoped in [m21b17_b1_relative_clauses_complete_scope.md](m21b17_b1_relative_clauses_complete_scope.md), closing the two things `gr:relative-clauses` (batch 13) itself deferred. **Object-relative clauses**: when the antecedent is the clause's object rather than its subject, the verb needs a resumptive pronoun pointing back at it — الْكِتَابُ الَّذِي قَرَأْتُهُ جَمِيلٌ ("the book that I read is beautiful"), reusing the exact attached-pronoun suffixes batch 16 just taught on إنّ's family, now landing on a verb instead of a particle. **Plural relative pronouns**: الَّذِينَ (new, masc. plural, human) and اللَّاتِي (new, fem. plural, human) apply `gr:noun-number`'s already-taught sound-plural pattern to already-known singulars (مُعَلِّم → مُعَلِّمُونَ, مُعَلِّمَة → مُعَلِّمَاتٌ) rather than inventing new plural-noun vocabulary; both example sentences use a prepositional-phrase predicate specifically to avoid needing an untaught plural verb conjugation. **Dual relative pronouns explicitly named, not taught** — genuinely rare and themselves case-inflected, the same "flag, don't teach" treatment Form IX and إنّ's 1st-person irregularity got. `gr:relative-clauses` grown additively (same `gr:` id, two new rule paragraphs, `examples` grown 4→8, two new `commonErrors` entries, `prereqs` gains `gr:attached-possessive` + `gr:noun-number`). One new lesson, `b1-relative-clauses-2`, `b1-u2`'s fifth. `b1-u2` blurb states subject/object and singular/plural relative clauses complete; only dual forms and paragraph-joining remain open. `npm run content:check` clean at 614 objects (412 lexemes, 21 grammar points, 120 texts, 38 units, 68 lessons); `npm run qa` 83/83; `node tools/a11y-audit.js` clean. ✅ **Batch 18 (joining ideas into paragraphs, closing `b1-u2`)** — scoped in [m21b18_b1_paragraphs_scope.md](m21b18_b1_paragraphs_scope.md), the unit's last named topic. **0 new lexemes, 0 new grammar points** — a reading-comprehension capstone, not new content: one connected paragraph (`txt:read-para-teacher-trip`, the same `"paragraph"` textType batch 8 introduced) puts the whole unit's toolkit to work in four sentences — a subject-relative clause with a nominal predicate, `gr:kana`, three sisters of إنّ (لٰكِنَّ, أَنَّ, لَعَلَّ) with attached-pronoun subjects, and the A2 connectors ثُمَّ/أَخِيرًا — built entirely from vocabulary already taught. Checked word-by-word against the lexicon before writing (an early draft used four words that turned out not to exist and was rewritten around confirmed vocabulary instead of adding new lexemes), and two natural-sounding but ungrounded constructions were deliberately avoided: attaching a pronoun to a preposition (not what `gr:attached-possessive` actually teaches — its suffixes attach to nouns) and an existential "there is" clause under a sister of إنّ (a genuinely contested parse per `reference/iraab-audit.md`'s own C1/C2 category, not safe to teach silently at B1). A second paragraph was drafted and dropped rather than lean on an unverified broken plural or an untaught possessive suffix (`ـنَا`/`ـهُمْ`) — one well-verified paragraph over two shaky ones, per this project's own quality-over-quantity philosophy. One new lesson, `b1-paragraphs`, `b1-u2`'s sixth and closing lesson, with comprehension questions (main idea, detail, sequence) rather than grammar-recognition, matching batch 8's own framing. `b1-u2` blurb now states the unit complete; dual relative pronouns are named as a permanent, deliberate scope boundary — the same framing `b1-u1`'s blurb gives Form IX — not a pending item. `npm run content:check` clean at 615 objects (412 lexemes, 21 grammar points, 121 texts, 38 units, 69 lessons); `npm run qa` 84/84; `node tools/a11y-audit.js` clean. ✅ **Batch 19 (attached possessive pronouns, plural forms, `b1-u2`)** — scoped in [m21b19_b1_attached_possessive_plural_scope.md](m21b19_b1_attached_possessive_plural_scope.md), closing the gap the session handoff note flagged: `gr:attached-possessive` only taught 5 of the paradigm's 8 everyday suffixes. **0 new lexemes.** Three new suffixes — ـنَا (our), ـكُمْ (your, plural), ـهُمْ (their) — extend `gr:attached-possessive` additively (same `gr:` id, `examples` grown 3→6, one new `commonErrors` entry, `prereqs` gains `gr:noun-number`); ـكُمْ/ـهُمْ are framed as the masculine/mixed-group forms, the same default convention the existing `هُمْ` lexeme note already established. Fem.-plural (ـكُنَّ/ـهُنَّ) and dual (ـكُمَا/ـهُمَا) suffixes are named, not taught — neither has an independent-pronoun counterpart anywhere in the lexicon either, so this stays the same "flag, don't teach" treatment Form IX and dual relative pronouns got. **Deliberately stays an A1-level grammar-point extension**, not bumped to B1, even though the gap surfaced writing B1 content — the paradigm itself is foundational A1 grammar; the new lesson lives in `b1-u2` anyway since that's where the gap was found. Broken plurals were explicitly NOT scoped alongside this — per the same session's discussion, that's a vocabulary-annotation operation spanning future batches, not a bounded grammar lesson. 3 new example texts, 1 new lesson (`b1-attached-possessive-plural`, `b1-u2`'s seventh). `npm run content:check` clean at 618 objects (412 lexemes, 21 grammar points, 124 texts, 38 units, 70 lessons); `npm run qa` 85/85; `node tools/a11y-audit.js` clean. ✅ **Batch 20 — the first B2 content: The Full Case System, first look (`b2-u1`)** — scoped in [m21b20_b2_case_system_first_look_scope.md](m21b20_b2_case_system_first_look_scope.md). The actual payoff of the whole M21.6→M21.7→batches 14–19 arc: the `label` engine's role+case tier, built and QA'd since M21.6 but never exercised by real content until now. **0 new lexemes, 0 new texts** — `case` was added directly to the 9 existing `parse` entries across `b1-role-recognition`'s own seven texts (an additive optional field), reusing the exact same sentences rather than authoring near-duplicates. One new grammar point, `gr:case-system`: states رَفْع/نَصْب/جَرّ and their markers, and explicitly frames the whole lesson as a *naming*, not new grammar — every case ending in it was already required by `gr:idafa`/`gr:kana`/`gr:inna-sisters`, taught back at A2/B1. Scoped deliberately narrow: regular (triptote) nouns only, with diptotes, the five nouns, sound-plural case markers, and verb mood all named in the rule text as a follow-up layer, not taught here. One new lesson, `b2-case-system-first-look`, `b2-u1`'s first: 5 `label` steps asking role **and** case together (the combined tier `exGenLabel` has supported since M21.6), plus 2 `practice-choice` reinforcement questions on the two double-tagged iḍāfa sentences. `b2-u1` flips `planned` → `available` — **the first B2 unit to open.** `npm run content:check` clean at 619 objects (412 lexemes, 22 grammar points, 124 texts, 38 units, 71 lessons); `npm run qa` 86/86 (including `b1-role-recognition` still passing unchanged, confirming the additive `case` field didn't alter its role-only behavior); `node tools/a11y-audit.js` clean. ✅ **Batch 21 — B2 case system, second look (الْأَسْمَاء الْخَمْسَة، الْجَمْع السَّالِم)** — scoped in [m21b21_b2_case_system_second_look_scope.md](m21b21_b2_case_system_second_look_scope.md), following directly from batch 20's own deferral. Teaches two of the three irregular case-marking layers batch 20 named but didn't teach: **the five nouns** (أَب/أَخ — the only two of the five already in the lexicon, using a long vowel و/ا/ي instead of a short one as the first term of an iḍāfa) and **the sound plurals'** own case markers (sound masculine plural's ـِينَ covering both نَصْب and جَرّ; sound feminine plural's unexpected kasra in نَصْب, identical to its own جَرّ form). **Diptotes remain deliberately deferred** — their indefinite/definite behavior needs a source check this session's effort budget didn't extend to, so it was named as a real gap rather than guessed at. **0 new lexemes.** 5 new texts; 2 existing texts (batch 17's sound-plural relative-clause examples) gain an additive `parse` field. `gr:case-system` grown additively — same `gr:` id, same pattern `gr:inna-sisters` used across batches 14–16 — rather than new grammar-point ids. One new lesson, `b2-case-system-second-look`, `b2-u1`'s second. `npm run content:check` clean at 624 objects (412 lexemes, 22 grammar points, 129 texts, 38 units, 72 lessons); `npm run qa` 87/87 (one real catch along the way: the new lesson's title first cited two full Arabic noun phrases in a plain-English string, tripping the lang/dir coverage check's 50%-Arabic threshold — the same class of issue M21.7 hit — fixed by citing only the three markers being taught instead); `node tools/a11y-audit.js` clean. ✅ **Batch 22 — B2 case system, third look (مَمْنُوع مِنَ الصَّرْف)** — scoped in [m21b22_b2_case_system_third_look_scope.md](m21b22_b2_case_system_third_look_scope.md), closing the gap batch 21 deferred. **Verified the indefinite/definite diptote rule against external sources before authoring** — a diptote takes fatḥa instead of kasra for جَرّ only when indefinite; الـ or إِضَافَة to a definite noun restores the regular kasra. **0 new lexemes** — `مَكَاتِب` (plural of `lex:wrk-01`) and `أَصْدِقَاء` (plural of `lex:peo-11`), both identified as candidates back in batch 21. 4 new texts, including a deliberate minimal pair (`هَذَا بَيْتُ أَصْدِقَاءَ` indefinite/fatḥa vs. `هَذَا بَيْتُ الْأَصْدِقَاءِ` definite/kasra) showing the exception to the exception directly. `gr:case-system` grown additively a third time — its rule text now covers the full case system on nouns (regular, five nouns, sound plurals, diptotes); only verb mood remains named as future work. One new lesson, `b2-case-system-third-look`, `b2-u1`'s third. `npm run content:check` clean at 628 objects (412 lexemes, 22 grammar points, 133 texts, 38 units, 73 lessons); `npm run qa` 88/88 clean on the first run (checked the new title's Arabic-character ratio before running QA this time, learning from batch 21's catch); `node tools/a11y-audit.js` clean. ✅ **Batch 23 — B2 present-tense mood, first look (أَنْ / لَنْ / لَمْ)** — scoped in [m21b23_b2_mudari_mood_first_look_scope.md](m21b23_b2_mudari_mood_first_look_scope.md), opening **`b2-u2`** ('The Passive, the Jussive & Conditionals') — the verb's own counterpart to `gr:case-system`'s completed noun-case arc. **Verified the mood system (which particle triggers which mood, how a sound verb marks each one) against external sources before authoring.** Unlike the three case-system batches, this one could not stay at zero new vocabulary — the lexicon had no mood-triggering particles at all, and no grammar point covered negating a past-tense VERBAL sentence (only `gr:negation-a1`'s nominal-sentence negation existed). **3 new lexemes** (أَنْ, لَنْ, لَمْ) and 3 new texts, all built on the already-established `ذَهَبَ`/`يَذْهَبُ` example verb. **Checked the `label` engine's schema first and found it has no jussive (`jazm`) case value** — built for noun case only (M21.6) — so this batch deliberately teaches mood through `explain`/`reading-practice`/`practice-choice` steps only, zero engine changes, rather than force-fitting an engine extension into a content batch. One new grammar point, `gr:mudari-mood` (a new id, not grown from `gr:case-system`, since the two are explicitly different systems). One new lesson, `b2-mudari-mood-first-look`. **`b2-u2` flips `planned` → `available`, with its `prereqs` corrected** from the speculative M16-era `["b1-u3"]` (participles — genuinely needed for this unit's still-unbuilt passive sub-topic, but not for mood) to the real, already-satisfied `["a2-u1"]`. `npm run content:check` clean at 635 objects (415 lexemes, 23 grammar points, 136 texts, 38 units, 74 lessons); `npm run qa` 89/89; `node tools/a11y-audit.js` clean. ✅ **Batch 24 — B2 two more accusative roles (الْحَال، التَّمْيِيز، لَا النَّافِيَة لِلْجِنْس)** — scoped in [m21b24_b2_hal_tamyiz_scope.md](m21b24_b2_hal_tamyiz_scope.md), `b2-u1`'s fourth lesson (`B2-iʿrāb-4` in the audit's table). **Verified against external sources before authoring — critically, the لا-النافية-للجنس مُعرب/مَبني distinction**: a single-word اسم لا is grammatically built (مَبني) with a fatḥa that only LOOKS accusative, not truly case-declined — getting this backwards would have taught a real inaccuracy, and `reference/iraab-audit.md` §3 names المعرب/المبني as this whole programme's biggest structural gap. **0 new lexemes** — حال uses already-known verbs/adjectives (`رَجَعَ`/`جَاءَ`, `سَعِيد`/`حَزِين`); تمييز and لا-النافية-للجنس reuse two EXISTING texts wholesale (`txt:gram-num-20`, `txt:gram-neg-no-water`) — both already demonstrated the mechanic correctly, just unnamed, the same "already doing this correctly" pattern `gr:case-system` established. 2 new texts for حال; 3 new grammar points (`gr:hal`, `gr:tamyiz`, `gr:la-nafiya-jins`, matching the audit's own indicative ids). One new lesson, `b2-hal-tamyiz-first-look` — `explain`/`reading-practice`/`practice-choice` only, no `label`-engine change, same discipline as batch 23. Two authoring artifacts (a leaked self-correction in a text gloss and in `gr:hal`'s rule text) were caught and cleaned up before building, not left for QA. `npm run content:check` clean at 640 objects (415 lexemes, 26 grammar points, 138 texts, 38 units, 75 lessons); `npm run qa` 90/90; `node tools/a11y-audit.js` clean. ✅ **Batch 25 — B2 two of the five objects (مَفْعُول فِيهِ، مَفْعُول مَعَهُ)** — scoped in [m21b25_b2_maful_fihi_maahu_scope.md](m21b25_b2_maful_fihi_maahu_scope.md), `b2-u1`'s fifth lesson (part of `B2-iʿrāb-2` in the audit's table, narrowed to what's tractable without participle vocabulary). **Verified both مَفْعُول فِيهِ's implied-في behavior and مَفْعُول مَعَهُ's three conditions against external sources first.** **0 new lexemes** — reuses `صَبَاح`/`لَيْل`/`دَرَسَ`/`جَلَسَ`/`اِبْن`/`صَدِيق`. 4 new texts; 2 new grammar points (`gr:maful-fihi`, `gr:maful-maahu`). One new lesson, `b2-maful-fihi-maahu` — `practice-choice` only, same engine-change-free discipline as batches 23–24. **Caught two more leaked self-correction artifacts before building** (following batch 24's own catch of the same failure mode) — a case-label typo in a text gloss and a broken placeholder word in a grammar point's own commonErrors — by explicitly reading back every new string in full before `npm run content:write`, not relying on QA to catch prose errors it has no way to check. مَفْعُول لَهُ, مَفْعُول مُطْلَق, ظَرْف مَكَان, and the passive voice (still blocked on `b1-u3`) all stay explicitly named and deferred. `npm run content:check` clean at 646 objects (415 lexemes, 28 grammar points, 142 texts, 38 units, 76 lessons); `npm run qa` 91/91; `node tools/a11y-audit.js` clean. ✅ **Batch 26 — التوابع, first two (النَّعْت، عَطْف النَّسَق)** — scoped in [m21b26_b2_naat_atf_nasaq_scope.md](m21b26_b2_naat_atf_nasaq_scope.md), `b2-u1`'s sixth lesson, splitting the audit's `B2-iʿrāb-5` (all four تَوَابِع in one batch) to teach only the two tractable with existing vocabulary. **النَّعْت** (an adjective fully agreeing with its noun in definiteness/gender/number/case — reuses the exact `الْبَيْتُ الْكَبِيرُ` phrase `a1-grammar-structural` used back at A1 as a "not a sentence" contrast, now formally named) and **عَطْف النَّسَق** (a conjunction forcing two independent nouns to share one case). **0 new lexemes.** 4 new texts, 2 new grammar points (`gr:naat`, `gr:atf-nasaq`). **Caught a lang/dir coverage risk before running QA this time**: the draft title computed to an exact 50% Arabic ratio by the QA harness's own formula — read `tools/qa-harness.js`'s check directly (line 277) rather than re-estimating, then shortened the Arabic citation to bring it safely under threshold before the suite ever ran. التوكيد and البدل (still blocked on vocabulary — نفس/عين; a clean substitution pair) stay named and deferred. `npm run content:check` clean at 652 objects (415 lexemes, 30 grammar points, 146 texts, 38 units, 77 lessons); `npm run qa` 92/92 clean on the first run; `node tools/a11y-audit.js` clean. | M20 |
| **M21.5** | Analytics & learning insights | A local-first-only Progress-view panel on M19/M19.5's event data: time-per-strand, accuracy trend, weakest-N objects (reuses M19's own `reviewQueue("weak")`), recent-activity chart, JSON export (progress + M19.5's full IndexedDB history). Inline-SVG, no charting library, synchronous render. Cross-learner aggregate/telemetry explicitly deferred to M22+. | ✅ merged (PR #14) — see [m21.5_analytics_learning_insights_scope.md](m21.5_analytics_learning_insights_scope.md), [content/m21.5-analytics-insights-review.md](content/m21.5-analytics-insights-review.md) — `tools/qa-harness.js` 31/31, `tools/a11y-audit.js` clean | M19.5, M21 |
| **M21.9** | Backend foundation (Supabase) | Backend decision locked in: **Supabase**. `supabase/schema.sql` — `profiles` / `enrollments` / `lesson_attempts` / `exercise_responses` (nullable `audio_accuracy`, nullable `expected_case`/`produced_case`/generated `case_error`) / `subscriptions`, RLS on every table. Stripe via two Edge Functions (`create-checkout-session`, `stripe-webhook`) for M23. Infrastructure only — no auth UI, no client sync, no paywall; `index.html` untouched. | 🧪 implemented, pending review — see [m22_backend_foundation_scope.md](m22_backend_foundation_scope.md), [content/m22-backend-foundation-review.md](content/m22-backend-foundation-review.md), [supabase/README.md](supabase/README.md) — schema + RLS verified against a real local Postgres 16 (clean apply, full cross-user isolation smoke test, `case_error` derivation through all 3 states); Edge Functions executed for real via `supabase/testing/functions/` (real `stripe`/`@supabase/supabase-js` SDKs + a local mock server, since `deno.land`/`esm.sh`/`supabase.com`/`api.stripe.com` are all network-policy-blocked in this sandbox — 21/21 assertions passing, including real HMAC signature verification) — a deploy against an actual live Supabase + Stripe project is still owed before M23 ships a paywall, blocked by that same network policy rather than by anything in the code | M19, M21.5 |
| **M22** | Student accounts / personal state | Email + password auth (Supabase Auth, per M21.9); cross-device sync of per-strand level, placement result, completed lessons, mistake + review history, vocabulary exposure, assessment history, preferences, onto M21.9's schema. `localStorage` stays the offline fallback. | Progress syncs across devices; a signed-out learner still works fully offline. | M19, M21.9 |
| **M23** | Subscriptions / monetisation | Premium tier (advanced course, native-audio pack, analytics, certificate) via Stripe, wired to M21.9's `create-checkout-session`/`stripe-webhook` Edge Functions and `subscriptions` table. Free vs. premium vs. teacher/professional decided here — **not** baked into the curriculum. | Payment gate works; the free tier is still a genuinely useful course. | M22 |

---

## After M23

| M | Name | Notes |
|---|---|---|
| **M24** | UI/UX & product refinement | A dedicated pass (likely a 3–4 part cluster): navigation, onboarding, placement UX, learner dashboard, lesson flow, mobile + desktop, Arabic typography, RTL/LTR, accessibility, writing interface, audio controls, progress visualisation, review experience, child / adult / advanced experiences, motion, feedback, consistency, performance. **Not interleaved with M15–M21.** The current "no UI change" rule on M14–M21 is about *sequencing*, not a verdict that today's UI is final. |
| **M25+** | AI personal tutor | A **layer over** the curriculum, never the curriculum. Needs M15 (learner model) + M16 (curriculum) + M18/M19 (assessment + mistake history) to do better than "let's practise Arabic." Example of what it can then do: *"This learner is Reading B1 but Listening A2; they recently struggled with past-tense conjugation and attached pronouns — today's conversation reinforces those without exceeding their vocabulary level."* |
| **M21.6** | Parse/label exercise-type engine | ✅ merged — the bounded engine prerequisite for the iʿrāb programme (a `label` exercise type, dormant until real content exists). See [m21.6_parse_label_exercise_type_scope.md](m21.6_parse_label_exercise_type_scope.md). This is the same milestone `m27.0_parse-engine_scope.md`/`m27_iraab_programme_scope.md` (still on disk, **now superseded — see their notes**) independently re-drafted, under a different name/number and a somewhat different design (`iʿrāb` annotation array + `term:`/`metalang.json` glossary), before the M21.6 history was recovered onto `main`. **M21.7** (drafted as PR #50) has since shipped — see the M21 row — the first real content lesson built on this engine ("Grammatical role recognition"). |

### Parallel tracks (not blocking the M-sequence)

- **Advanced / literary Arabic** — the C1/C2 rungs of M21, plus specialised units (الأدب، الشِّعر، البلاغة، الفكر والفلسفة، النثر، النصوص التراثية، الصحافة، اللغة الأكاديمية، الحجاج، النقد وتحليل النص). C1/C2 Arabic must have somewhere to go beyond "harder tourist phrases."
- **The iʿrāb programme** — teaching full grammatical parsing (مرفوع/منصوب/مجرور, معرب/مبني, محل الإعراب, the tawābiʿ, the أساليب) across B1→C2. Planned in [m27_iraab_programme_scope.md](m27_iraab_programme_scope.md) (umbrella, held, now superseded on its engine section — see the doc's own note) plus a sequence of `m21bN`/`m21.N`-style content batches, each mapped to an existing `b1-u*`/`b2-u*`/`c1-u1`/`c2-u1` unit stub. The engine gate is now satisfied (**M21.6**, merged) and B1 grammar is well underway (derived verb forms II–X, relative clauses); **M21.7** (drafted as PR #50, not yet merged) is the first real content batch against the engine. The two `iraablessons*.pdf` manuals are the teacher's reference for *what a correct parse looks like* — never a source of copied examples (`reference/iraab-audit.md`).
- **Dialect pathways** — Moroccan, Egyptian, Levantine, Gulf. Attach *alongside* the MSA spine any time after M16; never mixed into the foundational fuṣḥā course. `variety` field reserved since M14.
- **Teacher / classroom features** — assign lessons, track a class, review a cohort's mistakes. A product surface in its own right; **separate from M23 monetisation**, a candidate milestone after M22.
- **Community features** — per-lesson discussion threads, an honest non-gamified streak display, MVP keyword-filter moderation. The first proposed milestone that cannot be local-first: hard-depends on M22 (real identities), recommended after M23. No default cross-learner leaderboard (conflicts with the anti-gamification stance). Proposed as **M26** — see [m26_community_features_scope.md](m26_community_features_scope.md); tracked here rather than in the numbered core sequence since, like Teacher/classroom features above, it's optional and doesn't sit on the path every learner passes through. *(Requested as "M24" — renumbered because M24 is already committed above to UI/UX & product refinement.)*

### Continuous (never "done")

Content quality, new texts / dialogues / audio / exercises / explanations / assessments, and pedagogical iteration. There is no day when "Arabic is finished" — there is a point (≈ M19–M20) where the engine, curriculum architecture, assessment, and learner model are mature, and the platform can then grow indefinitely.

---

## Standing rules (apply to every milestone)

1. **Never invent Arabic.** Examples come from checked, real sources. Descriptors, level maps, and grammar rules are checked against external references, not authored from scratch.
2. **Learner-state migration discipline.** Any milestone that extends `progress` / learner-state (M15–M19, M22) ships a migration function + a before/after verification, as M14 did (`migrateMastered`, seeded-RNG byte-compare).
3. **Accessibility is a continuous constraint.** M15–M21 must not regress keyboard navigation, ARIA, RTL correctness, focus states, or contrast. The M24 pass polishes; it does not rescue.
4. **Honesty about un-gradable skills is permanent.** Free composition and speaking fluency are self-assessed against rubrics with a "compare to model" reference — never a fabricated number.
5. **Buildless runtime.** The shipped app keeps zero runtime dependencies and zero runtime fetches. Dev-time tooling (the `tools/*.js` compilers) is fine and expected.
6. **The Master Standards workflow** — Plan (scope doc, hold for approval) → Branch → Draft PR → QA (live browser trace) → merge only on explicit approval — applies to every milestone.
7. **Milestone vs. layer vs. operation.** A *milestone* ships a bounded deliverable. A *layer* (AI tutor) sits on top of finished milestones. An *operation* (content, quality) runs forever. Don't scope one as another.

## Explicitly deferred

- Backend / accounts / sync → **M22**. Any earlier is an empty shell.
- Dialect content → not until commissioned; never in the fuṣḥā core.
- Pronunciation *scoring* → the recorder stays "compare to model"; no score unless a genuinely reliable one exists.
- Full UI/UX redesign → **M24**, as one deliberate pass.
- AI-generated curriculum content → never. AI is a tutor layer, not a content factory.

---

## Session Handoff — 2026-09-15

*For whoever picks this up next, fresh session, no prior context. (Not put in `reference/` since
that folder is gitignored per CLAUDE.md and wouldn't survive a push — this note needs to.)*

### 1. What shipped this session

Started from a discovery: `main` had been silently force-pushed back to an earlier state on
2026-09-11 ("restore project on ubuntu"), discarding 10 merged PRs. Recovered the full lost state
onto `main` (parse/label engine, all derived verb forms, relative clauses), **restructuring** the
derived-verb-forms lessons from 2 crammed lessons into 8 (one per form) along the way — see
`m21b11-12_b1_derived_verb_forms_recovery_scope.md` for the full incident writeup.

From there, shipped M21.7 through M21 batch 18 in sequence, all on `main`, all verified with
`npm run content:check` / `npm run qa` / `node tools/a11y-audit.js` (each batch's own scope doc has
the exact numbers):

- **M21.7** — Grammatical role recognition (`b1-u7`), the label engine's first real content.
- **Batch 14** — إنّ وأخواتها, first look (إِنَّ / أَنَّ / لِأَنَّ).
- **Batch 15** — إنّ وأخواتها, completing the set (كَأَنَّ / لٰكِنَّ / لَعَلَّ / لَيْتَ).
- **Batch 16** — إنّ وأخواتها, attached-pronoun forms (إِنَّهُ / أَنَّهَا).
- **Batch 17** — Relative clauses, completing the set (object-relative + الَّذِينَ / اللَّاتِي).
- **Batch 18** — Joining ideas into paragraphs, closing `b1-u2`.

**`b1-u2` ("Relative Clauses & Longer Sentences") is now fully complete**: subject- and
object-relative clauses (singular + plural), all ten sisters of إنّ (noun or attached-pronoun
subject), and a closing paragraph-comprehension lesson. Dual relative pronouns are a deliberate,
permanent scope exclusion (same treatment `b1-u1` gives Form IX) — not a pending item.

Recurring lesson from this session, worth knowing up front: **`qa-harness.js`'s lang/dir coverage
check fails if a lesson title or feedback string is ≥50% Arabic characters without explicit
`lang="ar" dir="rtl"` wrapping.** Hit this twice (M21.7, batch 14) before internalizing it. Check
any new title/explanation string's Arabic ratio before running QA, not after.

### 2. Next decision points

Three independent options, none started, no PR or scope doc exists for any of them yet:

- **`b1-u3`** — Verbal Nouns & Participles (maṣdar, active/passive participles). `status: planned`,
  `prereqs: ["b1-u1"]` — already satisfied, ready to scope whenever.
- **`b1-u7` follow-up** — `b1-u7` currently has exactly one lesson (`b1-role-recognition`, M21.7).
  `LABEL_ROLES` in `index.html` only defines 5 roles (`mubtada`/`khabar`/`fail`/`mafulbih`/
  `mudafilayhi`); M21.6's own scope doc named extending this set (tawābiʿ, ḥāl, tamyīz, …) as a
  future content batch's job, not the engine's.
- **`b2-u1`** — The Full Case System (Iʿrāb) — real case-marking recognition/production at B2.
  `status: planned`, `prereqs: ["b1-u2", "b1-u7"]` — **both now satisfied for the first time**, so
  this is newly unblocked. This is arguably the most natural next step: it's the actual "iʿrāb"
  milestone the whole M21.6/M21.7/batches-14-18 arc has been building toward, and M21.6's own scope
  doc flagged it as reusing the exact same `label` mechanism with `case` now present on each
  `parse` entry — the two-tier design was built for exactly this reuse.

No lean recorded toward one over the others — genuinely the user's call, not a technical
dependency (all three are equally unblocked).

### 3. Open threads / concerns

- **Two stale draft PRs on GitHub should probably be closed**, since their content shipped directly
  to `main` instead of through them: **PR #49** (batch 14 scope — superseded by the batch 14 that
  actually shipped) and **PR #50** (M21.7 scope — superseded likewise). Left open, not touched, in
  case the user wants to look at them first.
- **PR #19** (M21.9 backend foundation — Supabase schema/RLS/Stripe) is still open, unrelated to the
  iʿrāb thread, "implemented, pending review" per its own ROADMAP row. Not touched this session.
- **`m27_iraab_programme_scope.md` and `m27.0_parse-engine_scope.md`** are still on disk, marked
  superseded (M21.6 shipped the same engine milestone under a different design before this history
  was recovered) but not deleted — kept for their planning/reasoning value (the 48-lesson book
  destination map in `m27_iraab_programme_scope.md` §3/§7 is still accurate). Worth a deliberate
  decision at some point: archive them, or fold their still-useful content (the destination map)
  into a doc that reflects the real M21.6/M21.7+ lineage.
- **A `git stash` entry is still sitting on this machine**: `WIP form IV lex additions (superseded
  by recovery)` — safe to drop (`git stash drop`) whenever, it predates the whole recovery and
  nothing in it survived.
- **Two known content gaps flagged during batch 18**, not blocking anything but worth remembering
  if a future batch wants to build B1 narrative paragraphs: no broken-plural vocabulary is taught
  yet (only sound plurals, via `gr:noun-number`), and `gr:attached-possessive` only teaches 5
  suffixes (`ـِي`/`ـكَ`/`ـكِ`/`ـهُ`/`ـهَا`) — no 1st/2nd/3rd-person plural ("our"/"your pl."/"their")
  suffixes anywhere yet.
